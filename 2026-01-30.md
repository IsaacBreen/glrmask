# 2026-01-30

## Summary
- Collected `PROFILE_PRECOMPUTE1_DFS=1` profiling results; DFS time is dominated by `expanded_weight_from_item()` (~13.07s) and `expanded_weight_from_rsb()` (~1.97s). Everything else is sub-100ms (`exec`, `possible_matches`, `add_transition`, `add_epsilon`, `tokens_accessible`, `expanded_all`).
- Verified post-minimize DWA stats logging appears; sample outputs include:
  - `TIMING: DWA post_minimize states=2408 transitions=9223`
  - `TIMING: DWA post_minimize states=4647 transitions=27006`
  - `TIMING: DWA post_minimize states=4278 transitions=56838`
- `run_dfs_profile.sh` now redirects output via `exec > profiling_output_dfs.txt 2>&1` (run via `bash` because execute bit was missing). Output captured in `profiling_output_dfs.txt`.

## DFS profiling details (PROFILE_PRECOMPUTE1_DFS=1)
- exec: 104,182 calls, 30.53ms
- possible_matches: 44,393 calls, 63.79ms
- tokens_accessible: 328 calls, 0.07ms
- expanded_item: 194,393 calls, 13,071.49ms
- expanded_rsb: 28,908 calls, 1,974.79ms
- expanded_all: 103,887 calls, 0.17ms
- add_transition: 253,587 calls, 50.08ms
- add_epsilon: 103,887 calls, 17.93ms
- Takeaway: DFS time dominated by `expanded_weight_from_item()` and `expanded_weight_from_rsb()`.

## Next steps
- Optimize `expanded_weight_from_item()` and `expanded_weight_from_rsb()` (primary DFS hotspots) to target ~5s total. Candidates: cache expanded weights, reduce `RangeSetBlaze` allocations, precompute token→weight ranges.
- Re-run `PROFILE_PRECOMPUTE1_DFS=1` after changes to confirm reductions.

## Hand-off details (for next agent)
- DFS profiling prints are gated by `MACRO_DEBUG_LEVEL >= 5` because `DfsProfile::print()` uses `crate::debug!(5, ...)`.
- `PROFILE_PRECOMPUTE1_DFS=1` enables the counters/timers; `MACRO_DEBUG_LEVEL=5` is required to see the output lines.
- Current script: `run_dfs_profile.sh` in repo root. It:
  - `cd` to repo
  - `exec > profiling_output_dfs.txt 2>&1` (so output is captured)
  - exports `MACRO_DEBUG_LEVEL=5`, `PROFILE_PRECOMPUTE1_DFS=1`, `PROFILE_ENABLED=1`
  - runs `make test-js`
- Output file: `profiling_output_dfs.txt`. To locate DFS timings, search for `precompute1 dfs profile` (lines were around ~4886 in the last run). To locate post-minimize stats, search for `TIMING: DWA post_minimize` (several matches).
- Dominant hotspots are in `src/constraint_precompute.rs`:
  - `expanded_weight_from_item()` (creates new `Weight` each call via `RangeSetBlaze::from_iter([start..=end])`)
  - `expanded_weight_from_rsb()` (calls `expand_rsb(&rsb, self.num_tsids)` then `Weight::from_rsb`)
  - Both are called in the DFS hot loop (see `run_dfs()` -> `dfs()` path).
- `expanded_weight_all()` is *not* a cost center (0.17ms total) even though it is called 103,887 times.
- DWA post-minimize stats appear multiple times (likely for different passes or inputs):
  - states=2408 transitions=9223
  - states=4647 transitions=27006
  - states=4278 transitions=56838

## Master notes
- Verified `projects-masteragent5.agent.md` matches its `.bak` (diff exit code 0). `.bak` timestamp: 11:47 today.
- Created full backups of all projects-* prompt files under `/tmp/prompt_backups/`.
- Restored Terminal Safety sections in `projects-subagent2.agent.md` and `projects-subagent2-2.agent.md` earlier.
- Prompt variants `projects-subagent3` / `projects-subagent3-2` and `projects-masteragent6` edits are paused pending the next session.

## DFS optimization update (precompute1)
- Implemented `expanded_item_cache` memoization in `Precomputer1` to reuse expanded single-token weights (avoids per-call `RangeSetBlaze::from_iter` + `Weight::from_rsb`).
- Changed `expanded_weight_from_rsb()` to take `&RangeSetBlaze` and updated call site to pass `final_bv.as_ref()` (avoids cloning in the weight-heavy path).
- Files modified: `src/constraint_precompute.rs`.
- Status: profiling complete; DFS totals now ~2.23s.
- Results: `expanded_item` 158.87ms, `expanded_rsb` 1,960.43ms (vs 13,071ms and 1,975ms baseline).
- Expected impact: target achieved (≤5s precompute1 DFS time).

## Pass2 Cancellations Optimization
- Implemented worklist de-duplication in `resolve_negatives.rs` using `FxHashSet` for `in_queue`
- Cancellations time: 15.78s → 10.78s (32% faster)
- Steps per call: 6437 → 4899
- Status: **Committed**

## Batch Cancellation Attempt (FAILED - needs revisiting)
- Idea: Do all cancellations AFTER pass2 completes, rather than per-range
- First attempt by worker failed with 68 test failures
- Worker cited "dependencies via right_body epsilons" as reason
- **HUMAN disagrees** - insists it SHOULD be equivalent
- **TODO**: Re-investigate implementation. May have been done incorrectly.

## DWA_FAST_MINIMIZE Removal
- Removed the `DWA_FAST_MINIMIZE` env-var fast path that was silently defaulting to partition refinement
- Tests pass with removal
- Status: Uncommitted, ready to commit

## Pending TODOs

### TODO: Batch negative resolution after pass2
HUMAN insists this should be equivalent and work correctly. Prior attempt may have been implemented wrong.
Need to revisit and understand why 68 tests failed.

### TODO: Make exact minimization effective
- "Exact" minimization paradoxically produces MORE states than "greedy" in some cases
- Root cause: height-0 over-approximation in incompatibility graph
- HUMAN says terminal DWA should be ~200 states, currently 2,369
- Need to investigate and fix

### TODO: Consider BOTH minimizations
- HUMAN idea: Run fast minimization first (for speed), then exact (for optimal count)
- May get best of both worlds

### TODO: Address size-based fast paths (7 identified)
Worker-2 audit found 7 hidden size-based fast paths in `dwa_acyclic/mod.rs`:
1. `graph_coloring.rs`: >30 nodes → greedy fallback
2. height-0 && >1000 → direct coloring
3. >200 && signature_coverage >70% → signature-only coloring
4. >30 candidates → greedy even with graph built
5. compatible_sig_pairs >50% && num_sigs >50 → interval heuristic
6. signature_coverage >50% in greedy_color_without_graph → signature-only

Should be made explicit via config or removed for predictable behavior.

---

## Session 2 (Late Evening)

### Commits Made

| Commit | Description |
|--------|-------------|
| `03a6671c0` | Optimize queries structure in cancellations: replace outer HashMap with Vec |
| `809511549` | Add are_compatible cache, fix partition_minimize height handling, remove two-pass path |
| `5706a7f22` | Add two-pass minimization mode and fix partition_minimize |
| `c52c71fff` | Remove threshold-based minimization fast paths, add idempotence test |
| `6dd22cdf2` | Add SKIP_SERIALIZATION flag, add parser DWA minimize verification |
| `60883c5ec` | Refactor DWA minimization: rename Minimize to ExactMinimize, add RustfstMinimize option |

### Threshold Removal
- Removed all size-based fast paths from minimization
- All minimization now uses exact graph coloring algorithm
- Tests pass (423 passed, 28 ignored)

### Two-Pass Minimization (Attempted & Abandoned)
- Idea: Run fast partition-based minimization, then exact coloring
- Implementation: Added `DWA_TWO_PASS_MINIMIZE=1` env var
- Result: **Slower than exact-only** on large DWAs
- Profiling showed `partition_minimize` takes ~9.5s but only reduces states by 13%
- **Decision: Removed two-pass runtime path**

### are_compatible Optimization
- Added thread-local cache for `targets_equivalent_on_domain`
- Reduced from ~50s to ~7.5s (477K calls at 15.7µs avg)
- **Committed**

### Cancellations Optimization: Vec for queries
- Replaced outer `FxHashMap<NWAStateID, FxHashMap<...>>` with `Vec<FxHashMap<...>>`
- Confirmed faster on smaller grammars (3.7s → 0.8s for temp_inline.ebnf)
- **Committed**

### Rustfst Experiments
- Tested rustfst minimization at different pipeline stages
- **Key finding**: After our exact coloring minimization, rustfst finds NO further reductions
- This confirms our minimization produces minimal state count

### SKIP_SERIALIZATION Flag
- Added `SKIP_SERIALIZATION=1` env var to skip JSON output
- Reduces compile time from ~6min to ~3.6min
- Added to Makefile test targets by default
- **Committed**

### Minimization Refactor
- Renamed `DwaPass::Minimize` → `DwaPass::ExactMinimize`
- Added `DwaPass::RustfstMinimize` variant
- All minimization now configurable via `DeterminizeAndMinimizeProfile`
- **Committed**

### Parser DWA Idempotence
- Added `VERIFY_PARSER_DWA_MINIMIZE=1` check
- Verified minimization is idempotent: 13,407 → 13,407 states
- **Committed**

### Current State Counts (js.ebnf)

| DWA | States | Notes |
|-----|--------|-------|
| Terminal (after suffix prune) | 476 | ✓ Expected ~500 |
| Super | 4,428 | |
| Parser | 12,662-13,407 | Varies by run |

### Timing Breakdown (js.ebnf with SKIP_SERIALIZATION=1)

| Phase | Time |
|-------|------|
| **Total** | ~277s (~4.6 min) |
| build_parser_dwa | 263s |
| ├─ complex_signatures | 72s |
| ├─ specialize_dw_as | 67s |
| ├─ NWA::determinize | 67s |
| ├─ DWA Minimize | 50s |
| └─ pass2 (cancel+finality) | 48s |
| Terminal DWA total | ~11s |

### JSON Serialization Investigation
- `serde_json::to_vec` takes ~166s for js.ebnf output
- Output: 356 MB compressed JSON
- Streaming to gzip: **slower** (didn't complete in 600s)
- simd-json: **slower** (199s vs 166s)
- Decision: Keep serde_json, use SKIP_SERIALIZATION for iteration

### Files Modified
- `src/dwa_i32/minimization/dwa_acyclic/mod.rs` - threshold removal, are_compatible cache
- `src/dwa_i32/minimization/dwa_acyclic/partition_minimize.rs` - height-based fix
- `src/dwa_i32/minimization/common.rs` - DwaPass refactor
- `src/dwa_i32/minimization_config.rs` - RustfstMinimize option
- `src/precompute4/resolve_negatives.rs` - Vec optimization for queries
- `src/bin/grammar_compiler.rs` - SKIP_SERIALIZATION flag
- `Makefile` - SKIP_SERIALIZATION default in test targets
