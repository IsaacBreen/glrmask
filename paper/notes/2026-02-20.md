# 2026-02-20

## Summary

- Set suffix pruning to global default-off with env opt-in in `grammars2024`.
- Ran JSON-only CFA sweep (`sep1` + `llguidance`, no `xgrammar`) and confirmed mismatches exist in Snowplow case `sp_13_Normalized`.
- Added interruption-safe partial-result persistence for CFA sweep runs.
- Removed `%ignore WS` from `schema_to_lark` (sep1-target schemas) and reran the same JSON sweep; mismatch counts were unchanged in this sample.

## Key Commits

- `grammars2024`: `4773b9055` — disable suffix pruning by default in make test targets.
- `grammars2024`: `19415b6f5` — make suffix pruning opt-in via env flag.
- `constraint-framework-analysis`: `1b0cad2` — persist partial CFA sweep output on interrupt.

## JSON Sweep Snapshot

Run: `scripts/sweep.py --source jsb --sample-per-category 2 --frameworks sep1 llguidance --max-examples-per-problem 1`

- attempted: 20
- ok: 17
- skipped_no_examples: 3
- total tokens: 3469
- discrepancy token-events: 118
- sampled discrepancy accuracy:
  - `sep1`: 0/100
  - `llguidance`: 100/100

Primary mismatch cluster in this sample: `jsb/Snowplow/sp_13_Normalized`.

## Simplest Reported Discrepancy (from sampled set)

- `problem_id`: `jsb/Snowplow/sp_13_Normalized`
- `token_id`: `2430` (`","`)
- prefix tail context ends at: `..."notification":{"attachments":[{"identifier":"`
- votes: `ground_truth=True`, `llguidance=True`, `sep1=False`

## Notes

- CFA sweep now writes partial JSON output on `SIGINT`/`SIGTERM` once the run loop has started.
- For sep1-target schema grammar generation, `%ignore WS` has been removed; whitespace is no longer globally ignored there.
