[package]
name = "glrmask-browser-artifact"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
glrmask = { path = ".." }
glrmask-runtime = { path = "../glrmask-runtime" }
serde_json = { version = "1", features = ["preserve_order"] }
