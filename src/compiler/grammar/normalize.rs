

// SEP1_MAP: This normalization hook is a compiler-local wrapper around the grammar-cleanup work sep1 spreads across `glr/analyze.rs` and `interface/optimization.rs`.

pub use crate::compiler::glr::analysis::normalize_for_mask;