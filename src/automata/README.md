# Automata

Automata are grouped by denotation: lexical byte automata, ordinary unweighted automata, and weighted automata. Width-specific names such as `weighted_u32` are now compatibility aliases rather than canonical module names.
