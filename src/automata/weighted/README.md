# Weighted automata

Weighted automata own generic algorithms over labels and `sets::weight::Weight`: determinization, minimization, serialization, and graph construction. They should not know about JSON Schema, runtime state, or Python bindings.
