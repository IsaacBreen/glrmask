# Mapped artifact and compaction

This directory owns coordinate changes for weighted compiled artifacts. Compaction must rewrite every `Weight` reference consistently and must preserve the denotation of the artifact under the quotient id map.
