interface:
  display_name: "Debug GLRMask Mask"
  short_description: "Trace glrmask mask/commit root causes"
  default_prompt: "Use $debug-glrmask-mask to trace a glrmask mask/commit mismatch to its root cause."
