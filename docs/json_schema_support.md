# JSON Schema support

This document should define the supported JSON Schema subset precisely before release.

## To document

- Supported schema drafts or draft-like assumptions
- Supported scalar types
- Object properties, required fields, additional properties, and property ordering behavior
- Array constraints
- String constraints, regex behavior, and Unicode/UTF-8 assumptions
- Numeric constraints
- `anyOf`, `oneOf`, `allOf`, `not`, and exact-subtraction behavior
- Unsupported keywords and failure modes
- Examples and regression tests for every supported construct
