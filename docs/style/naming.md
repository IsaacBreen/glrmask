# Naming policy

Use paper terminology: Terminal DWA, Parser DWA, Scan relation, CanMatch set, Grammar IR, Commit, Mask, graph-structured stack, and template DFA. Use `can` for exact reachability predicates and reserve `may` for conservative approximations. Use `internal`/`original` for token-id quotients.
