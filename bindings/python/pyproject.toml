[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "glrmask"
version = "0.1.0"
requires-python = ">=3.8"
description = "Fast grammar-constrained decoding for LLMs"

[tool.maturin]
manifest-path = "Cargo.toml"
features = []
